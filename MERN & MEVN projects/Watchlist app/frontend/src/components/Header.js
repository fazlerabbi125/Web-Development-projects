const Header = ({header}) => {
    return ( 
        <header className="text-center mt-2 mb-4" >
            <h1 className="display-5 banner">{header}</h1>
        </header>

     );
}
 
export default Header;