const Header = ({ header }) => {
    return (
        <header className="text-center mb-5" >
            <h1 className="display-5 banner">{header}</h1>
        </header>

    );
}

export default Header;